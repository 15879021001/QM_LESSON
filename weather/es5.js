'use strict';

var num = 9;
var isMarried = false;

var yourTurn = ['好工作', '找老板', '驴妈妈网'].map(function (tesk, index) {
    return index + '-' + tesk;
});
console.log(yourTurn);

// es6=>es5
// const、let -> varbal
